import { cardTitle } from "assets/jss/material-dashboard-pro-react.js";

const inquiryTypes = {
  cardIconTitle: {
    ...cardTitle,
    marginTop: "15px",
    marginBottom: "0px",
  },
};

export default inquiryTypes;
